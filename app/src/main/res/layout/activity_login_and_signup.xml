<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_login_and_signup"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:background="@drawable/fixit_app_home"
    tools:context="com.example.slash.fixit_2.LoginAndSignupPkg.LoginAndSignup">

    <Button
        android:text="SIGN UP FREE"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="140dp"
        android:id="@+id/signup"
        android:textColor="#cccccc"
        android:textSize="16dp"
        android:padding="16dp"
        android:background="@drawable/background_login"
        android:layout_alignParentBottom="true"
        android:backgroundTint="#bb000000"
        android:onClick="openSignup"/>

    <Button
        android:text="LOG IN"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="29dp"
        android:id="@+id/login"
        android:textColor="#222222"
        android:textSize="16dp"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:background="@drawable/background_login"
        android:backgroundTint="#bbffbb33"
        android:onClick="openLogin"/>

    <TextView
        android:text="Already a user?"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textColor="#ffffff"
        android:background="@android:color/transparent"
        android:layout_marginBottom="18dp"
        android:id="@+id/textView"
        android:layout_above="@+id/login"
        android:layout_centerHorizontal="true" />

</RelativeLayout>
